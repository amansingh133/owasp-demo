{
  "name": "owasp-compliance-demo",
  "version": "1.0.0",
  "scripts": {
    "build": "cd frontend && npm install && npx vite build && rm -rf ../backend/public && mkdir -p ../backend/public && cp -r dist/* ../backend/public/",
    "start": "cd backend && node server.js",
    "install-all": "npm install --prefix backend && npm install --prefix frontend",
    "postinstall": "npm run install-all"
  }
}
